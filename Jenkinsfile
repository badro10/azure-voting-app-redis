pipeline {
   agent any

   stages {
      stage('Vérify Branch') {
         steps {
            echo $GIT_BRANCH
         }
      }
      stage('Goodbye') {
         steps {
            echo 'Goodbye World'
         }
      }
   }
}
